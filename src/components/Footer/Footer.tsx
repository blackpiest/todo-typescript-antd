import React from 'react'
import { Footer } from 'antd/lib/layout/layout'

const FooterSite = (props: any) => {
    return(
        <Footer style={{ textAlign: 'center' }}>Разработано при помощи Typescript, Ant Design и React.</Footer>
    )
}

export default FooterSite